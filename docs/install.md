# Install

Editable install:

```bash
pip install -e .
```

Optional extras:

```bash
pip install -e ".[astro,viz,ml,sim]"
```

Docs tooling:

```bash
pip install -e ".[docs]"
python -m mkdocs serve
```
