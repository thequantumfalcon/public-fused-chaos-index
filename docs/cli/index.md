# CLI

Discover commands:

```bash
fci --help
fci tier1 --help
fci tier2 --help
```

All Tier-2 runners are artifact-driven and write:
- `*_results.npz`
- `*_manifest.json`

See: Artifacts → NPZ Contracts / Manifests.
